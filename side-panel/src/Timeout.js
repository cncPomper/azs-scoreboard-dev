import React from "react";
import { Link } from "react-router-dom";

const Timeout = ({ to }) => {
  return (
    <>
      {/* <Link to={`/timeout/${timeout.id}`}>
        <div className="Timeout" >
          {to}
        </div>
      </Link> */}
    </>
  );
};

export default Timeout;